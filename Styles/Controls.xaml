<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 命名样式（叠加 Setter，不破坏 WinUI 默认控件模板） -->

    <!-- 卡片容器：圆角 + 主题边框 -->
    <Style x:Key="CardBorder" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource CardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
    </Style>

    <!-- 工具栏按钮：统一圆角与尺寸，颜色由各按钮 Foreground/Background 单独指定 -->
    <Style x:Key="ToolbarButton" TargetType="Button">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Padding" Value="10,0"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- 工具栏切换按钮（置顶） -->
    <Style x:Key="ToolbarToggleButton" TargetType="ToggleButton">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Padding" Value="10,0"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Background" Value="{ThemeResource SubtleControlBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource SubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- 工具栏下拉框 -->
    <Style x:Key="ToolbarComboBox" TargetType="ComboBox">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Background" Value="{ThemeResource SubtleControlBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource SubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

</ResourceDictionary>
